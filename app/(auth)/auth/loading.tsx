import React from 'react';

// app/loading.tsx


const Loading: React.FC = () => (
  <div style={{
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    height: '100vh',
    fontSize: '1.5rem'
  }}>
    Loading...
  </div>
);

export default Loading;